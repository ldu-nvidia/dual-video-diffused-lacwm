# Reproducible runtime for Python 3.10, PyTorch 2.7.1, CUDA 12.8, and B200.
#
# The local VideoX-Fun checkout is deliberately not installed from PyPI. Prepare
# the pinned source with tools/env/prepare_videox_fun.sh and source
# tools/env/activate_b200.sh before importing the model.
--extra-index-url https://download.pytorch.org/whl/cu128

# GPU runtime. The local version tags force pip to use the CUDA 12.8 wheels.
torch==2.7.1+cu128
torchvision==0.22.1+cu128

# Wan / diffusion stack. huggingface-hub 0.30.2 is not compatible with
# diffusers 0.38.0, which requires huggingface-hub >=0.34.0.
diffusers==0.38.0
accelerate==1.6.0
peft==0.19.1
safetensors==0.8.0
huggingface-hub[cli]==0.36.0
transformers==4.57.6
tokenizers==0.22.2
timm==1.0.19
ftfy==6.3.1
librosa==0.11.0
sentencepiece==0.2.1

# Configuration, distributed launch, and training framework.
hydra-core==1.3.2
omegaconf==2.3.0
colorlog==6.9.0
einops==0.8.1
kornia==0.8.2
torchdata==0.11.0
wandb==0.27.0

# Dataset and media I/O.
numpy==2.0.1
scipy==1.15.3
pandas==2.2.3
pyarrow==24.0.0
h5py==3.12.1
opencv-python==4.11.0.86
decord==0.6.0
av==17.1.0
imageio==2.37.0
imageio-ffmpeg==0.6.0
matplotlib==3.10.3

# ABC preprocessing and evaluation.
mcap==1.4.0
mcap-protobuf-support==0.5.4
lpips==0.1.4

# Validation of the provisioned checkout.
pytest==8.4.1
