# @package _global_
defaults:
  - /experiments_0908/action_token_common
  - _self_

name: action-token-fixed-at-off-seed1234-u000200
model:
  action_token:
    enabled: false
wandb:
  tags: [abc, vpm, action-token, fixed-dose, control, seed-1234]
