# @package _global_
defaults:
  - /experiments_0908/frequency_forcing_common
  - _self_

name: frequency-fpm-video-only
model:
  dual_diffusion:
    condition_mode: "off"
    condition_on_tf: false
    condition_on_tf_clock: false
    schedule_mode: aligned
    tf_lead_logit: 0.0
    tf_loss_weight: 0.0
    state_gate_init: 0.0
    clock_gate_init: 0.0
    parameter_matched_control: true
wandb:
  tags: [abc, frequency-forcing, FPM, parameter-matched, seed-1234]
