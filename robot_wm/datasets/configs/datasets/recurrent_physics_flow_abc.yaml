_target_: robot_wm.datasets.multi_dataset.MultiDataset
padding_dim: 157
seed: ${seed}
infinite: true
img_augment: false
emit_crop_rgb: false
transform: {sample_size: 13, chunk_size: 5}
future_validity: {enabled: false, max_retries: 0}
datasets:
  ABC:
    _target_: robot_wm.datasets.abc.recurrent_physics_flow_dataset.ABCRecurrentPhysicsFlowDataset
    clip_manifest: ???
    cache_metadata: ???
    flow_metadata: ???
    expected_split: ???
    expected_clip_count: ???
    expected_manifest_sha256: ???
    expected_rgb_sha256: ???
    expected_actions_sha256: ???
    expected_flow_sha256: ???
    expected_confirmation_analysis_identity_sha256: ???
    expected_confirmation_completion_identity_sha256: ???
    seed: ${seed}
    infinite: ${dataset.infinite}
    transform: null
