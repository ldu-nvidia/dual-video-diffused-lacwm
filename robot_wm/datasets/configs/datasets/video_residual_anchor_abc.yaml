_target_: robot_wm.datasets.multi_dataset.MultiDataset
padding_dim: 157
seed: ${seed}
infinite: true
img_augment: false
emit_crop_rgb: false
transform: {sample_size: 13, chunk_size: 5}
future_validity:
  enabled: false
  history_frames: 5
  future_frames: 8
  temporal_ratio: 4
  num_views: 3
  view_std_threshold: 0.001
  max_retries: 0
datasets:
  ABC:
    _target_: robot_wm.datasets.abc.video_residual_anchor_dataset.ABCVideoResidualAnchorDataset
    clip_manifest: ???
    cache_metadata: ???
    expected_split: ???
    expected_clip_count: ???
    expected_manifest_sha256: ???
    expected_rgb_sha256: ???
    expected_actions_sha256: ???
    seed: ${seed}
    infinite: ${dataset.infinite}
    transform: null
